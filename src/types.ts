export type Language = "da" | "pa";

export interface Dictionary {
  ones: string[];
  teens: string[];
  tens: string[];
  hundreds: string[];
  scales: string[];
  conjunction: string;
  currency: string;
  decimal: string;
  zero: string;
}